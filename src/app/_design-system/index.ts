/**
 * Design System Barrel
 * Re-exports all tokens and helpers for convenient importing.
 *
 * @example
 * import { colors, manaColors, getRankTier } from '@/app/_design-system'
 */

export {
  colors,
  fonts,
  manaColors,
  rankColors,
  spacing,
  getColorIdentityGradient,
  getRankTier,
  type ManaColor,
  type RankTier,
} from './tokens/tokens'
